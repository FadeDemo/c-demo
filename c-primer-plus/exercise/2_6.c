#include <stdio.h>
int main(void) {
    int toes = 10;
    int twice = toes * 2;
    int square = toes * toes;
    printf("toes is %d, twice is %d, square is %d", toes, twice, square);
    return 0;
}